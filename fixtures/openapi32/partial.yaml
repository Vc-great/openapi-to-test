openapi: 3.2.0
info:
  title: OAS 3.2 Partial Consumer
  version: 1.0.0
$self: https://example.test/openapi.yaml
paths:
  /health:
    get:
      operationId: getHealth
      responses:
        "204":
          description: healthy
