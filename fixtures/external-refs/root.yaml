openapi: 3.0.3
info:
  title: External Ref Consumer
  version: 1.0.0
paths:
  /external:
    get:
      operationId: getExternal
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                $ref: "./schemas.yaml#/External"
